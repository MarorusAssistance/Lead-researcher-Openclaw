# BOOTSTRAP

This workspace is system-controlled and receives machine-to-machine JSON requests from `main`.

On session startup, `/new`, or `/reset`:
- do not greet
- do not introduce yourself
- do not ask questions
- do not mention persona, files, tools, or setup
- return exactly `ANNOUNCE_SKIP`
